{
  "config.json": "{\n  \"CHAT_CHANNEL_ID\": 1502290312737853532,\n  \"STATUS_CHANNEL_ID\": 1500890747715588097\n}",
  "status.py": "import json\nimport requests\nimport os\n\ndef load_config():\n    \"\"\"Loads configuration from config.json\"\"\"\n    config_path = os.path.join(os.path.dirname(__file__), 'config.json')\n    with open(config_path, 'r') as f:\n        return json.load(f)\n\ndef send_to_discord(channel_id, message):\n    \"\"\"Sends message to Discord via Local API Gateway\"\"\"\n    url = 'http://localhost:5000/send'\n    payload = {\"channel_id\": channel_id, \"content\": message}\n    try:\n        response = requests.post(url, json=payload)\n        return response.status_code == 200\n    except Exception as e:\n        print(f\"Failed to reach API: {e}\")\n        return False\n\ndef main():\n    config = load_config()\n    status_channel = config.get('STATUS_CHANNEL_ID')\n    \n    # Example logic: Get system status\n    msg = \"Bot Status: System Online on Termux\"\n    \n    if status_channel:\n        success = send_to_discord(status_channel, msg)\n        if success:\n            print(\"Status reported successfully.\")\n\nif __name__ == '__main__':\n    main()",
  "crontab_setup": "# To add this to crontab, run 'crontab -e' and add this line:\n# * * * * * cd /path/to/your/script && /usr/bin/python3 status.py"
}